<h1>New Review for <%= @restaurant.name %></h1>
<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :rating, as: :select, collection: (0..5).to_a %>
  <%= f.input :content %>
  <%= f.submit "Submit Review" %>
<% end %>
